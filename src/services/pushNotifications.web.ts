/** Web stub — remote Expo push is not used on web. */
export async function registerExpoPushToken(): Promise<string | null> {
  return null;
}

export async function maybeRegisterExpoPushToken(): Promise<string | null> {
  return null;
}
